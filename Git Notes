$ git config --global user.email
$ git config --global user.name "chandan-devs"
$ git config --global user.email "chandangupta.devs@gmail.com"
$ git config --global user.email
$ git config --global user.name
$ git init
$ ls -lart
$ git add -A
$ git commit -m"commit name"
$ git checkout "file name"
$ git checkout -f
$ git log 
$ git log -p -2
$ git diff 
$ git diff --staged
$ git commit -a -m "commit name"
$ git rm --cached "file name"
$ git rm "file name"
$ touch .gitignore
$ git branch "branch name"
$ git branch
$ git checkout "branch name"
$ git merge "branch name"
$ git checkout -b "branch name"
$ ssh-keygen -t ed25519 -C "chandangupta.devs@gmail.com"
$ eval "$(ssh-agent -s)"
$ ssh-add ~/.ssh/id_ed25519
$ cat ~/.ssh/id_ed25519.pub
$ git push -u origin master
$ git checkout flaskIntegration
$ git push -u origin flaskIntegration
$ git push
$ cd /clone
$ git clone https://github.com/xing4git/blog.git chax

